<% form_for(@ticket) do |f| %>
  <%= f.error_messages %>
  <dl>
    <dt><%= f.label :title %></dt>
    <dd><%= f.text_field :title %></dd>
  </dl>
  <%= render :partial => submit_partial %>
<% end %>

require File.expand_path(File.dirname(__FILE__) + '/../../spec_helper')

describe "/tickets/_change" do
  it "should contains comment"
  #it "should contains comment" do
    #@change.stub!(:comment).and_return "comment must be here"
    #render '//ticket_change'

    #response.should have_tag('p', :text => @change.comment)
  #end

  it "should contains assignee change"
  #it "should contains assignee change" do
    #@change.stub!(:comment).and_return "comment must be here"
    #render '/notifier/ticket_change'

    #response.should have_tag('p', :text => @change.comment)
  #end
end
